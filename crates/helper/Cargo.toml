[package]
name = "os-switcher-helper"
version = "0.1.0"
description = "Minimal privileged helper for os-switcher: performs default/next/clear boot writes."
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[[bin]]
name = "os-switcher-helper"
path = "src/main.rs"

[dependencies]
os-switcher-core.workspace = true
